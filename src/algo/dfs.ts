
const depthFirstSearchGraph = (arr: number[], target: number): number => {
    const stack = [];
    
    
 
    return -1;
};

const depthFirstSearchTree = (arr: number[], target: number): number => {
    const stack = [];



    return -1;
};

export { depthFirstSearchGraph, depthFirstSearchTree };